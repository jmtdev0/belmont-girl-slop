# Local-only development setting. Copy this file to .env.local and adjust it
# to the folder that contains the video files on your own computer.
BELMONTGIRL_VIDEO_DIR=C:/ruta/a/belmontgirl_videos

# Public R2 source for the video catalog. Leave empty to use the local folder.
VITE_VIDEO_BASE_URL=https://bgvideos.bethecandle.live

# Optional: use a custom FFmpeg executable when generating palettes.
# FFMPEG_PATH=ffmpeg
